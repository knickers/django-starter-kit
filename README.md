# Django starter kit

```shell
touch .env
```

```shell
./setup.sh
```

```shell
docker compose up -d
```

```shell
./shell python -V
./shell python -m django --version
```

```shell
http://localhost
```
